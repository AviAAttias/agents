conductor:
  enabled: false
spring:
  datasource:
    url: jdbc:h2:mem:reconciliationworker_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/migration/h2
